services:
  xlxd:
    image: ghcr.io/eekdood/xlxd-container:latest
    container_name: xlxd
    restart: unless-stopped
    network_mode: host

    environment:
      DASHBOARD: dashboard1
      XLXNUM: XLX999
      XLXD_BIND: 0.0.0.0
      XLXD_AMBE_IP: 127.0.0.1
      APACHE_PORT: 80
      CALLHOME: "0"
      EMAIL: admin@example.com
      URL: https://xlx.example.com
      COUNTRY: your_country
      DESCRIPTION: your_comment
      MODULEA: Int.
      MODULEB: Regional
      MODULEC: National
      MODULED: ""
      TZ: America/New_York

    volumes:
      - ./xlxd/config:/xlxd-config
      - ./xlxd/data:/xlxd-data
